/**
 * Home page — Intro + About on a single scrollable route ("/").
 * See design.md §4.1 and §4.2.
 */
export default function Home() {
  return (
    <main>
      <section id="intro">
        {/* Phase 2: Intro section — Logo, Hero heading, subtext, CTA */}
      </section>
      <section id="about">
        {/* Phase 2: About section — scroll-revealed, two-column layout */}
      </section>
    </main>
  );
}
