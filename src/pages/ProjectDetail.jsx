/**
 * Project detail page — dynamic template driven by data/projects.js ("/projects/:slug").
 * Uses useParams() to find the project by slug.
 * See design.md §4.4.
 */
export default function ProjectDetail() {
  return (
    <main>
      {/* Phase 4: Navbar, back link, project hero, body content, demo button */}
    </main>
  );
}
