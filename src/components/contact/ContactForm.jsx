/**
 * Contact form — name, email, message fields.
 * Posts to the Express backend at VITE_API_URL/api/contact.
 * See design.md §4.5 and backend.md §5.
 */
export default function ContactForm() {
  return (
    <form>
      {/* Phase 5: Form fields + submit */}
    </form>
  );
}
