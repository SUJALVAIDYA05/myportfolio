/**
 * Single project grid card — arch-framed screenshot + title + blurb.
 * Links to /projects/:slug. See design.md §4.3.
 */
export default function ProjectCard() {
  return (
    <div>
      {/* Phase 3: Card content */}
    </div>
  );
}
