/**
 * Hand-drawn underline flourish SVG — used after "Projects" heading.
 * Animates in with a "draw-on" effect (stroke-dashoffset).
 * See design.md §3.4 and animations.md §4.
 */
export default function Squiggle() {
  return (
    <svg aria-hidden="true">
      {/* Phase 3: Squiggle SVG path */}
    </svg>
  );
}
