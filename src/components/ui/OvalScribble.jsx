/**
 * Hand-drawn oval scribble SVG — circles around "About" heading.
 * Animates in with a "draw-on" effect (stroke-dashoffset).
 * See design.md §3.4 and animations.md §4.
 */
export default function OvalScribble() {
  return (
    <svg aria-hidden="true">
      {/* Phase 2: Oval scribble SVG path */}
    </svg>
  );
}
