/**
 * Reusable arch-topped image frame.
 * Flat sides + semicircular top, overflow hidden, object-fit cover.
 * See design.md §3.3.
 */
export default function ArchImage({ src, alt, className = '' }) {
  return (
    <div className={className}>
      {/* Phase 2: Arch-framed image */}
    </div>
  );
}
