/**
 * Persistent navigation bar — shown on /projects, /projects/:slug, /contact.
 * Logo (left, links to /) + text links "Projects" / "Contact" (right).
 * See design.md §3.2.
 */
export default function Navbar() {
  return (
    <nav>
      {/* Phase 2: Logo + nav links */}
    </nav>
  );
}
